<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".DetailActivity"
    android:background="@color/black"
    android:orientation="vertical">

    <com.google.android.youtube.player.YouTubePlayerView
        android:id="@+id/player"
        android:layout_width="match_parent"
        android:layout_height="250dp" />


    <TextView
        android:id="@+id/tvTitle"
        android:textColor="@color/white"
        android:layout_width="match_parent"
        android:fontFamily="sans-serif"
        android:textStyle="normal|bold|italic"
        android:layout_height="wrap_content"
        android:textSize="32sp"
        tools:text="it chapter two" />

    <RatingBar
        android:id="@+id/ratingBar"
        style="@style/Widget.AppCompat.RatingBar.Indicator"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:isIndicator="true"
        android:numStars="10"
        android:rating="8.5" />

    <TextView
        android:id="@+id/tvOverview"
        android:textColor="@color/white"
        android:fontFamily="sans-serif"
        android:textStyle="normal|bold|italic"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:text="27 years after overcoming the malevoltent supernatural entity pennywise the former memeber of the losers clucb who have grown up and moved away from derry are brouight back togereher bya devsdtating phone call" />
</LinearLayout>